name: finance_components
description: Finance-specific components — amount input, balance masking, OTP/PIN, transaction status, receipt cards.
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  design_tokens:
    path: ../design_tokens
  cupertino_components:
    path: ../cupertino_components

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
